@inject SignInManager<IdentityUser> SignInManager
@{
    var hasExternalLogins = (await SignInManager.GetExternalAuthenticationSchemesAsync()).Any();
}

<ul class="menu w-full space-y-2">
    <li>
        <a asp-page="./Index"
           class="@(ManageNavPages.IndexNavClass(ViewContext) == "active" ? "bg-primary text-primary-content" : "") flex items-center gap-3 rounded-lg hover:bg-transparent">
            <i data-lucide="user" class="w-4 h-4"></i>
            Profile
        </a>
    </li>
    <li>
        <a asp-page="./ChangePassword"
           class="@(ManageNavPages.ChangePasswordNavClass(ViewContext) == "active" ? "bg-primary text-primary-content" : "") flex items-center gap-3 rounded-lg hover:bg-transparent">
            <i data-lucide="key-round" class="w-4 h-4"></i>
            Password
        </a>
    </li>
    @*@if (hasExternalLogins)
    {
        <li>
            <a asp-page="./ExternalLogins"
               class="@(ManageNavPages.ExternalLoginsNavClass(ViewContext) == "active" ? "active" : "") gap-2">
                <i data-lucide="log-in" class="w-4 h-4"></i>
                External Logins
            </a>
        </li>
    }
    <li>
        <a asp-page="./TwoFactorAuthentication"
           class="@(ManageNavPages.TwoFactorAuthenticationNavClass(ViewContext) == "active" ? "active" : "") gap-2">
            <i data-lucide="shield" class="w-4 h-4"></i>
            Two-Factor Auth
        </a>
    </li>
    <li>
        <a asp-page="./PersonalData"
           class="@(ManageNavPages.PersonalDataNavClass(ViewContext) == "active" ? "active" : "") gap-2">
            <i data-lucide="database" class="w-4 h-4"></i>
            Personal Data
        </a>
    </li>*@
</ul>
